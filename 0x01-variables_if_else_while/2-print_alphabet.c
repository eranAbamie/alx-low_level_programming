#include <stdlib.h>
/**
*main - prints the alphabet in lower case followed by a new line
* Return : Always 0
*/
int main (void)
{  char ch;
for (ch = 'a'; ch <= 'z'; ch++)
putchar('\n');
return (0);
}
